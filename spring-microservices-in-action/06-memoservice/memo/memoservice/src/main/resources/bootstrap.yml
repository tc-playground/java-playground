# contains application service name, uri, and other info.

spring:
  application:
    name: memoservice
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
#  eureka:
#    instance:
#      preferIpAddress: true
#    client:
#      registerWithEureka: true
#      fetchRegistry: true
#      serviceUrl:
#          defaultZone: http://localhost:8761/eureka/